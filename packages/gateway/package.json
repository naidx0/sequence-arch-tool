{
  "name": "@sequence/gateway",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Standalone OpenRouter-backed model gateway for Sequence's free default tier — holds the funded key, enforces the global spend backstop.",
  "license": "MIT",
  "bin": {
    "sequence-gateway": "dist/server.js"
  },
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
