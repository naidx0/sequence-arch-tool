server {
    listen 80;

    location /api/ {
        proxy_pass http://gateway:3000/api/;
    }

    # trap: no proxy_pass here — must not produce an edge
    location /health {
        return 200;
    }
}
