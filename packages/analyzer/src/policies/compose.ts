/**
 * Policy composition — re-exported from schema so the analyzer read/check path
 * and locking tests share one implementation with the web Accept gate.
 */

export { composePolicies } from '@sequence/schema';
