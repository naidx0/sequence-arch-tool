{
  "name": "@sequence/api-types",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "description": "The shared HTTP wire contract for the Sequence platform server. Types only — no runtime code.",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@sequence/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
