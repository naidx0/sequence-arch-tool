## 2026-09-10

Built from `f8388b9a` — first public release, 2351 commits.

### the most recent 60 changes
- ci: the lock probe asks WMI on purpose, and that is now held
- ci: the counting gate needed a newer Node than the project claims
- site: a stranger could not find the installer from the site
- ci: the public mirror ran tests whose subject the mirror deletes
- release: three private references were about to be published, two of them mine
- release: build the macOS DMG on a machine that can build one
- ci: the sandbox driver's take and release had never once run
- desktop: the installer wore Electron's logo, because nothing named an icon
- lookup:located — a symbol lookup lights the part that holds it
- ci: gate:count answered differently depending on which shell launched it
- teach lane: a dependency's worker is not a test, and could not clean up
- ci: the sandbox lock is one file, shared, and syncs between machines
- ci: a sandbox driver that refuses rather than collides
- teach lane: the test suite left 598,408 directories in %TEMP%
- desktop: the smoke gate could not pass on the state it most exists to check
- gate: a compiled test whose source is gone is a counting failure — it cost another lane a false red
- codeforge lane: first run said "Choose reasoning in Sett…" and then opened an empty menu
- desktop: the Windows installer builds at 179 MiB, and two barriers stand before the app on both OSes
- site: point the public links at naidx0/sequence-arch-tool
- tools: release-public.mjs - export main to the public repo as one release commit
- docs: the marketing site is live from site/, not retired — the note has been wrong since 2026-08-26
- codeforge lane: the board can light a lookup — and the wire event cannot land from one side
- teach lane: symbol indexing reaches ONE of the four, so the criterion I wrote named the wrong fix
- teach lane: the shipped default draws 12 of 12 too, and its fabrication number is not zero
- codeforge lane: my harness hand-listed four routes of forty-four, and the missing one reached a screenshot
- teach lane: the symbol-index criterion, registered rather than patched
- teach lane: the four split into two faults, and the lookup fix is not available
- codeforge lane: every canvas chart test cited a node id, and the path that cites none is the one now load-bearing
- teach lane: 12 of 12 drawn across three general questions, and the gate's 4-of-13 false positives
- teach lane: the general-knowledge draw fires on 4 of 13 real lessons, so the caption stops claiming absence
- codeforge lane: seven of ten packages were nameless rectangles, and both obvious fixes are already refused here
- teach lane: "what is machine learning" is drawn — 4 of 4, and the fabrication is gone
- codeforge lane: the whole repository has a door, and it is not a card
- codeforge lane: the teach row claimed an outcome, and the rule against that was one line below it
- codeforge lane: I reported a defect that is not there, and the invariant it denied is now locked
- teach lane: the ruled gate would refuse 12 of 13 real lessons, and the defect is the verb
- teach lane: the artefact — a fabricated lesson beside a canvas that says it drew nothing
- codeforge lane: the section the last commit's message described was never written
- codeforge lane: 83% was 86.5%, and the picture that answers the brief has no entry point
- teach lane: the literal question drawn 0 times in 6, and one run in three invented a repo fact
- codeforge lane: I looked at it, and two of my own sections were wrong
- teach lane: the pair, and which half is allowed to lose
- codeforge lane: the anatomy view had never been opened in a browser
- teach lane: the refusal scored both ways, and the obvious fix breaks the half nobody was measuring
- codeforge lane: the one cell that is not a thing in the repository now says so
- teach lane: the refusal's own kill number was counting false positives as successes
- codeforge lane: the label rule's own arithmetic was the only thing checking it
- teach lane: the refusal named a file from the wrong repository
- codeforge lane: a real repository on the board draws one connector, and the obvious fix is already refused
- teach lane: the drawing works — the owner's example has nothing to draw
- codeforge lane: two blocks on one canvas, and the pair had a defect neither block had
- teach lane: the learner's answer is never read, and the two-turn contract is off by default
- codeforge lane: the test that listed its own subjects missed one
- teach lane: the contract holds on one turn end to end, and the product supplies all three
- codeforge lane: a fenced code block could still push the document sideways
- codeforge lane: the measure test could not see the cascade
- codeforge lane: canvas controls show keyboard focus
- ask: the turn deadline had no caller, so the experiment section 6 asks for could not be run
- codeforge lane: prose gets the book's reading measure, and the backlog closes
- teach: --teach on the CLI now refuses a subject-less lesson instead of paraphrasing the digest

### and 2291 earlier commits, not listed

Development ran from 2026-04-28 to 2026-09-10. This mirror carries no development
history by design — it is one squashed commit per release — so the 2291 commits
before the list above are summarised by this line rather than reproduced.

